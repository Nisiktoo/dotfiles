#include <bits/stdc++.h>

using namespace std;
using ll = long long;

#ifdef DEBUG
#include <debug.h>
#else 
#define dbg(x...)
#endif

void solve() {

}

int main() {
        ios::sync_with_stdio(false);
        cin.tie(nullptr);
        int _t = 1;
        cin >> _t;
        for (int i = 1; i <= _t; i++) {
                solve();
        }
        return 0;
}
